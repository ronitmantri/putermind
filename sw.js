// A simple service worker to satisfy PWA requirements
self.addEventListener("fetch", (event) => {
  // This is empty on purpose for now.
  // It just lets the browser know we have a worker.
});
